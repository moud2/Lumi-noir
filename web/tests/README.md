# E2E tests

## Setup
- Install dependencies: `npm install`
- Install browsers: `npx playwright install`

## Run
- `npm run test:e2e`

## Admin flow (optional)
Set these env vars to enable the admin test:
- `E2E_ADMIN_EMAIL`
- `E2E_ADMIN_PASSWORD`
